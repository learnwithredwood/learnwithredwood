{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "devDependencies": {
    "@redwoodjs/core": "0.50.0"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": ">=14.19 <=16.x",
    "yarn": ">=1.15"
  },
  "prisma": {
    "seed": "yarn rw exec seed"
  },
  "packageManager": "yarn@3.2.0",
  "dependencies": {
    "node-gyp": "^9.0.0"
  },
  "scripts": {
    "plop": "npx plop",
    "contentlayer": "yarn workspace api run contentlayer",
    "db:test": "docker run --rm --name learn-redwood-test -p 5433:5432 -e POSTGRES_PASSWORD=learn -e POSTGRES_DB=learn_test postgres:14-alpine",
    "db": "docker run --rm --name learn-redwood -p 5432:5432 -e POSTGRES_PASSWORD=learn -e POSTGRES_DB=learn_dev postgres:14-alpine"
  }
}
